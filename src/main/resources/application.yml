server:
  port: 8761

## Eureka Server Configuration
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: localhost
  server:
    renewal-percent-threshold: 0.90